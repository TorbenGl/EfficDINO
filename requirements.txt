# Base requirements — works on Windows (CPU) and Linux (CPU)
# For Linux GPU training, use requirements-server.txt instead
torch>=2.2.0
torchvision
omegaconf
torchmetrics
fvcore
iopath
submitit
einops
wandb
timm
datasets>=2.18.0
huggingface_hub>=0.21.0
